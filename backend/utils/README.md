# utils/ — Shared Utilities

Small, stateless, reusable helper functions with no business meaning of their own (e.g. date formatting, file hashing, text chunking helpers for document processing, logging configuration). Anything that starts to encode business rules belongs in `domain/` or `services/` instead, not here.
