# logs/ — Application Log Output

Local destination for application log files during development. In production, logs would be shipped to a centralized log aggregator rather than written to disk here.

Contents are gitignored — only this README and `.gitkeep` are tracked.
