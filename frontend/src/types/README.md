# types/ — TypeScript Type Definitions

Shared TypeScript interfaces/types mirroring backend `schemas/` (e.g. `Policy`, `Conflict`, `User`, API response envelopes). Keeping these centralized avoids type duplication across components and services.
