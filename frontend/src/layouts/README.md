# layouts/ — Page Shell Templates

Wrapper components that define the overall page structure for groups of routes (e.g. `AuthenticatedLayout` with sidebar+navbar vs `PublicLayout` for login/register). Pages are rendered inside a layout via routing configuration.
