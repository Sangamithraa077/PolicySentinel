# hooks/ — Custom React Hooks

Reusable stateful logic extracted from components (e.g. `useAuth`, `usePolicyUpload`, `useConflictData`, `usePagination`). Hooks encapsulate data-fetching and UI-state logic so components stay focused on rendering.
