# pages/ — Route-Level Views

Top-level components mapped directly to application routes (e.g. Dashboard, PolicyUpload, ConflictExplorer, Login). Pages compose `components/` and `layouts/`, fetch data via `services/` and `hooks/`, and hold page-specific state only.
