# contexts/ — React Context Providers

App-wide state shared across the component tree via React Context (e.g. `AuthContext` for the current user/JWT, `ThemeContext`). Used sparingly — only for truly global state; page-local state stays in `pages/`.
