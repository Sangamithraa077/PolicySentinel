# components/common/ — Generic UI Primitives

App-agnostic, highly reusable components with no business meaning: buttons, inputs, modals, tables, badges, loaders. These should be usable in any React project, not just PolicySentinel.
